# Configuración de pnpm para seguridad
# Evita ejecución automática de scripts postinstall
enable-pre-post-scripts=false

# Usa store estricto para mejor seguridad
store-dir=~/.pnpm-store

# Verifica integridad de paquetes
verify-store-integrity=true

# Auto install peers (recomendado para Next.js)
auto-install-peers=true

# Shamefully hoist deshabilitado por seguridad (evita problemas de resolución)
shamefully-hoist=false

